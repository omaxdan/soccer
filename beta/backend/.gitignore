node_modules/
dist/
.env
.env.local
.env.*.local
*.log
logs/
.DS_Store
.vscode/
.idea/
*.swp
*.swo
*~
.cache/
.next/
build/
coverage/
out/
.turbo/

# API sample logger output (backend/src/utils/apiSamples.ts) - raw debug
# dumps of real API responses, several hundred KB per file, meant for
# local reference during development only. One of these (squad/*.json
# for 7 countries) was accidentally committed at ~7.3MB total in commit
# a134609 - keep the README documenting the mechanism, ignore the dumps
# themselves.
docs/api-samples/**/*.json
!docs/api-samples/README.md

# Committed by mistake in e3ed901 — real API keys and a service key.
# Untracked here; the values still exist in history and must be rotated.
.env2
.env*.txt
